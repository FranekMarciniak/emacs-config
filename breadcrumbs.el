;; so far breadcrumb-mode does not have a straight package. Run this shell command:
;;
;; wget https://raw.githubusercontent.com/joaotavora/breadcrumb/master/breadcrumb.el --output-document ~/.emacs.d/breadcrumb.el
;;


(load "~/.emacs.d/breadcrumb.el")

(breadcrumb-mode 1)
